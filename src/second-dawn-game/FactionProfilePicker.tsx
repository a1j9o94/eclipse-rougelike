import type {FactionProfile} from '../../shared/eclipse/catalog';
export default function FactionProfilePicker({value,onChange,disabled=false}:{value:FactionProfile;onChange:(profile:FactionProfile)=>void;disabled?:boolean}){
 return <fieldset className="dg-faction-profile"><legend>Faction collection</legend><div role="group" aria-label="Faction collection">{([{id:'expanded-v1',name:'Expanded civilizations',detail:'Base factions + Rho Indi, Magellan, Midas and Ragnarok'},{id:'base',name:'Base game only',detail:'Original paired civilization boards'}] as const).map(profile=><button type="button" key={profile.id} aria-pressed={value===profile.id} disabled={disabled} onClick={()=>onChange(profile.id)}><strong>{profile.name}</strong><small>{profile.detail}</small></button>)}</div><p>Choose the available civilizations. Game length and rule options are set below.</p></fieldset>;
}
