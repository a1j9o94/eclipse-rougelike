import type {FactionProfile} from '../../shared/eclipse/catalog';
export default function FactionProfilePicker({value,onChange,disabled=false}:{value:FactionProfile;onChange:(profile:FactionProfile)=>void;disabled?:boolean}){
 const legacy=value==='expanded-v1';
 return <fieldset className="dg-faction-profile"><legend>Faction collection</legend><div role="group" aria-label="Faction collection">{([{id:'expanded-v2',name:'Expanded',detail:legacy?'This existing room uses the earlier expanded roster (16 civilizations). Select to include Exiles and Lyra.':'All 18 civilizations, including Exiles and Lyra'},{id:'base',name:'Base',detail:'Original paired civilization boards'}] as const).map(profile=><button type="button" key={profile.id} aria-pressed={value===profile.id||(legacy&&profile.id==='expanded-v2')} disabled={disabled} onClick={()=>onChange(profile.id)}><strong>{profile.name}</strong><small>{profile.detail}</small></button>)}</div><p>Choose the available civilizations. Game length and rule options are set below.</p></fieldset>;
}
