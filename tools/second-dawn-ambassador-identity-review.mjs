import {chromium,webkit} from 'playwright';
import assert from 'node:assert/strict';
import {mkdir,writeFile} from 'node:fs/promises';
const origin=process.env.SECOND_DAWN_SITE_URL??'http://127.0.0.1:5173';if(!['localhost','127.0.0.1'].includes(new URL(origin).hostname))throw new Error('Use isolated local fixtures.');
const output='coding_agents/second_dawn_ambassador_identity_review';await mkdir(output,{recursive:true});const results=[];
for(const [engine,type]of [['chromium',chromium],['webkit',webkit]]){const browser=await type.launch();try{for(const [width,height]of [[1440,900],[390,844]]){const page=await browser.newPage({viewport:{width,height},hasTouch:width<900,reducedMotion:'reduce'});const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto(`${origin}/?position=diplomacy#second-dawn-preview`);const offer=page.getByRole('group',{name:'Ambassador offer from Eridani Empire'});await offer.waitFor();await offer.scrollIntoViewIfNeeded();assert.ok(await offer.getByRole('img',{name:'Eridani Empire emblem'}).isVisible());assert.ok(await offer.getByRole('heading').evaluate(e=>parseFloat(getComputedStyle(e).fontSize)>=20));await page.screenshot({path:`${output}/${engine}-${width}.png`});assert.equal(await offer.evaluate(e=>e.scrollWidth>e.clientWidth),false);assert.equal(await page.evaluate(()=>document.documentElement.scrollWidth>innerWidth),false);assert.deepEqual(errors,[]);results.push({engine,width,height,offeringFactionVisible:true,errors});await page.close();}}finally{await browser.close();}}
await writeFile(`${output}/results.json`,JSON.stringify(results,null,2));console.log(JSON.stringify(results));
